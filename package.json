{
  "name": "mistral",
  "private": true,
  "version": "1.0.0",
  "description": "Static homepage for Mistral media",
  "scripts": {
    "content:generate": "node scripts/generate-articles-content.mjs",
    "build": "npm run content:generate && node build-static.mjs"
  }
}
